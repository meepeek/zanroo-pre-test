module.exports = async function(app) {
  // var User = app.models.User;
  // User.create({email: 'me@meepeek.com', password: 'password'}, function(err, userInstance) {
  //   console.log(userInstance);
  // });

  // await Models.Role.create({
  //     name: 'admin'
  //   })
}
